# Define the class
class Node:
    def __init__(self, name, dOfU, dOfV):
        self.name = name
        self.dOfU = dOfU
        self.dOfV = dOfV
        neighbors = []

# get input
input = list

class q5.Graph:
    def __init__(self,nodesList):
        self.nodesList = nodesList
        distances = []
        for node in nodeList:
            distances.append(node)
    def dijekstra(start,end):
        distances[start] = 0
        while(unvisited.size >0){
            relax = unvisited.extractmin;
        for neighbor in relax.neighbors :
            if (distances[neighbor] > distances[start] + w[neighbor start]){
                distances[neighbor] = distances[start] + w[neighbor start]
            }
        }
        

